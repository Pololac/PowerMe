services:
  # ========================================
  # FRONTEND (Angular + Nginx)
  # ========================================
  frontend:
    image: registry.gitlab.com/pololac/powerme/frontend:latest
    container_name: powerme-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"
    networks:
      - powerme-network
    environment:
      - TZ=${TZ:-Europe/Paris}

  # ========================================
  # BACKEND (Spring Boot)
  # ========================================
  backend:
    image: registry.gitlab.com/pololac/powerme/backend:latest
    container_name: powerme-backend
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Paris}
      - SECURITY_JWT_SECRET=${SECURITY_JWT_SECRET}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USER}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS}
      - SPRING_JPA_HIBERNATE_DDL_AUTO=validate
      - SPRING_JPA_SHOW_SQL=false
      - MAIL_HOST=${MAIL_HOST}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
    depends_on:
      db:
        condition: service_healthy
    # PAS de ports = accessible uniquement via le réseau Docker
    networks:
      - powerme-network

  # ========================================
  # DATABASE (PostgreSQL + PostGIS)
  # ========================================
  db:
    image: postgis/postgis:16-3.4-alpine
    container_name: powerme-db
    restart: unless-stopped
    ports: # PAS de ports = accessible uniquement via le réseau Docker
      - "127.0.0.1:5432:5432" # pr accès admin local (via psql sur VPS)
    networks:
      - powerme-network
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - TZ=${TZ:-Europe/Paris}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local

networks:
  powerme-network:
    driver: bridge
